<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_pastel"
    tools:context=".activity_app.nickname.MyNameActivity">

    <!-- Action Bar -->
    <FrameLayout
        android:id="@+id/layoutActionBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent">

        <include
            android:id="@+id/actionBar"
            layout="@layout/layout_actionbar" />
    </FrameLayout>

    <!-- Input Field -->
    <EditText
        android:id="@+id/edtInputName"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/bg_myname_input"
        android:hint="@string/input_name"
        android:textColorHint="#535353"
        android:paddingHorizontal="20dp"
        android:textSize="18sp"
        android:textColor="@color/dark_purple"
        android:fontFamily="@font/roboto_regular"
        android:inputType="text"
        android:maxLines="1"
        app:layout_constraintTop_toBottomOf="@id/layoutActionBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Generate Button -->
    <TextView
        android:id="@+id/btnGenerate"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="60dp"
        android:background="@drawable/bg_button_rounded"
        android:text="@string/generate"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:fontFamily="@font/roboto_regular"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/edtInputName"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Custom Nickname Button -->
    <TextView
        android:id="@+id/btnCustomNickname"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/bg_button_rounded"
        android:text="@string/custome_nickname"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:fontFamily="@font/roboto_regular"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/btnGenerate"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>

